# Local DB Connection
#spring.application.name=backend
#spring.datasource.url=jdbc:mysql://localhost:3306/taskvantage?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#spring.datasource.username=root
#spring.datasource.password=root
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Azure DB Connection:
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
# Other Environmental Variables

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Logging configurations
logging.level.org.springframework.security=WARN
logging.level.com.taskvantage=WARN
logging.level.io.jsonwebtoken=WARN

# Log to console instead of file (Azure App Service captures console output)
logging.level.root=WARN
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN
logging.level.com.taskvantage.backend.service.NotificationService=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n